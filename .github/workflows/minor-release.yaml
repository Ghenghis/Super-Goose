# DISABLED: Scheduled release PR creation requires Block Inc's release tooling (hermit, just).
# Was running every Tuesday creating PRs that fail. Re-enable when release process is set up.
name: Create Minor Release PR (DISABLED)

permissions:
  contents: write
  pull-requests: write

on:
  workflow_dispatch:

jobs:
  release:
    if: github.repository == 'Ghenghis/Super-Goose'
    uses: ./.github/workflows/create-release-pr.yaml
    with:
      bump_type: "minor"
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
      XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
      TETRATE_API_KEY: ${{ secrets.TETRATE_API_KEY }}
